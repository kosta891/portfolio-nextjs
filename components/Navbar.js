import ThemeSwitch from './UI/ThemeSwitch';

export default function Navbar() {
  return (
    <header>
      <nav>
        <div>logo</div>
        <div>links</div>
        <button className='bg-gray-500 dark:bg-gray-400 '>button</button>
      </nav>
    </header>
  );
}
